# =============================================================================
# Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# -----------------------------------------------------------------------------
# Application URLs
# -----------------------------------------------------------------------------
VERCEL_URL=http://localhost:5173

# -----------------------------------------------------------------------------
# Database (Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Local development: Use Docker Postgres
# Production: Use Neon pooled connection string
# Format: postgres://user:password@host:port/database?sslmode=require&pooling=true
DATABASE_URL="postgres://root:mysecretpassword@localhost:5433/local"

# Optional: Force Neon serverless driver (auto-detected in production)
# USE_NEON_SERVERLESS="true"

# -----------------------------------------------------------------------------
# Redis (Upstash - Required for SSE pub/sub in production)
# -----------------------------------------------------------------------------
# Local development: Get free Upstash Redis at console.upstash.com
# Production: Use same Upstash Redis instance (HTTP-based, perfect for serverless)
# Get these from Upstash Console → Database → REST API section
UPSTASH_REDIS_REST_URL="https://your-region.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-token-here"

# -----------------------------------------------------------------------------
# Authentication (Better Auth)
# -----------------------------------------------------------------------------
# Generate a secure secret: openssl rand -base64 32
# BETTER_AUTH_SECRET="your-secret-here"
# BETTER_AUTH_URL="${VERCEL_URL}"

# -----------------------------------------------------------------------------
# AI / LLM Integration
# -----------------------------------------------------------------------------
OPENAI_API_KEY="your-openai-api-key-here"

# -----------------------------------------------------------------------------
# Web Push Notifications (VAPID Keys)
# -----------------------------------------------------------------------------
# Generate VAPID keys: pnpm dlx web-push generate-vapid-keys
# These keys allow your server to send push notifications to browsers
# PUBLIC_VAPID_KEY="YOUR_PUBLIC_VAPID_KEY"
# VAPID_KEY="YOUR_VAPID_KEY"

# Optional: Email for VAPID subject (mailto: or https:// URL)
# VAPID_SUBJECT="mailto:chris@useblip.dev"

# -----------------------------------------------------------------------------
# Tinybird (Events & Analytics)
# -----------------------------------------------------------------------------
# Get token from Tinybird Console: https://ui.tinybird.co/tokens
# For local use run: tb token copy "admin local_testing@tinybird.co" && TB_LOCAL_TOKEN=$(pbpaste)
TINYBIRD_TOKEN="your-tinybird-token-here"
TINYBIRD_API_URL="https://api.tinybird.co"

# -----------------------------------------------------------------------------
# QStash (Upstash Workflow) - For reliable background jobs
# -----------------------------------------------------------------------------
# Get these from: https://console.upstash.com/qstash
# Local development: Use local QStash CLI (pnpm dlx @upstash/qstash-cli dev)
QSTASH_TOKEN=
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# -----------------------------------------------------------------------------
# Optional: Analytics & Monitoring
# -----------------------------------------------------------------------------
# SENTRY_DSN="your-sentry-dsn"
