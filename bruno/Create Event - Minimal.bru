meta {
  name: Create Event - Minimal
  type: http
  seq: 4
  tags: [
    events
    v1
    minimal
  ]
}

post {
  url: {{api_url}}/events
  body: json
  auth: bearer
}

auth:bearer {
  token: {{api_key}}
}

headers {
  content-type: application/json
}

body:json {
  {
    "channelName": "system-logs",
    "title": "System Check Completed"
  }
}
